{
  "name": "@dailybee/tracker",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "Activity capture for DailyBee: active-window sampling and browser tab/URL capture through OS accessibility & automation (no browser extension), plus categorisation rules and aggregation.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  }
}
